// Regression test: array_fill with negative size should produce empty array,
// not crash via huge malloc (negative size reinterpreted as unsigned).
fn main() {
    var a = array_fill(-5, 42);
    var b = array_fill(0, 99);
    var c = array_fill(3, 7);
    return len(a) + len(b) + len(c);
}
