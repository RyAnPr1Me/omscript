// bigint.cpp — Non-inline implementations for the OmScript BigInt library.
//
// Most operations are header-only in include/bigint.h.
// This file exists as a compilation unit so that bigint.h can be included
// in multiple translation units without linker conflicts, and to allow
// future non-trivial implementations.
#include "bigint.h"
