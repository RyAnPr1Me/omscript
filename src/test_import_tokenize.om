import "lexer.om";

fn main() {
    var src = "fn f(x) { return x + 1; }";
    var ts = tokenize(src);
    println("count=" + to_string(ts.count));
    var i = 0;
    while (i < ts.count) {
        println(to_string(i) + ":" + ts.lexemes[i]);
        i = i + 1;
    }
    return 0;
}
