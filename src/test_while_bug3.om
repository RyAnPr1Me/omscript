global var g_x = 5;

fn get_and_dec() {
    g_x = g_x - 1;
    return g_x > 0;
}

fn main() {
    var cnt = 0;
    while (get_and_dec()) {
        cnt = cnt + 1;
        println("cnt=" + to_string(cnt));
    }
    println("final cnt=" + to_string(cnt));
    return 0;
}
